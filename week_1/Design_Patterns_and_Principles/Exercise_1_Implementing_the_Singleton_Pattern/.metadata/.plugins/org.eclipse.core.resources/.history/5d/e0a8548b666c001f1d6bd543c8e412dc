
public class Logger {
	private static Logger obj;
	private Logger(){
		
	}
	public static Logger getInstance() {
		if(obj==null) {
			new Logger();
		}
		return obj;
	}
	public void show(Logger log, String str) {
		System.out.println("Logger: "+str + "-> Address: " + log.hashCode());
	}
}
